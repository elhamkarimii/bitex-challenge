import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#26C6DA',
    },
    secondary: {
      main: '#ff5722',
    },
    mode: 'dark'
  },
})

export default theme
